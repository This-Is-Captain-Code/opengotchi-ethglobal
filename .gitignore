# Node
node_modules/
npm-debug.log*
.env
.env.local

# Secrets — server-side wallet keys live only here, never committed
**/wallets.json
*.key
blink_private.pem

# Editor / OS
.DS_Store
.vscode/
*.log
